name: app
on: [push]
jobs:
  building:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: |
          actions/checkout@v2
          actions/setup-node@v2
          JamesIves/github-pages-deploy-action@4.1.4
      
      - name: build              
        run: |
          yarn install
          yarn build
      
      - name: deploy
      - with:
          branch: gh-pages 
          folder: build
      
